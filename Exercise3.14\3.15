#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector<int> vec;
    int a;
    while (cin>>a)
        vec.push_back(a);
    return 0;
}


#include <iostream>
#include <string>
#include <vector>
using std::string;
using std::vector;
using std::cin;
using std::cout;

int main()
{
  vector<string> vec;
  for(string a;cin>>a;vec.push_back(a));
  return 0;
}
